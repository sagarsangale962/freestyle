version: 1.0
runtime: corretto11
build:
  commands:    
    build:
      - mvn clean package
run:                              
  command: java -Xms256m -war target/roshamboJavaApp-1.0-SNAPSHOT.war .
